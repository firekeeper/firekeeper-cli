#!/usr/bin/env node

'use strict'

const program = require('commander')

program
    .version(require('../package').version, '-v, --version')
    .usage('<command>')
    .command('init <mode>', '初始化项目')
    .parse(process.argv)

// 校验版本
// require('../lib/check-version')('simple')

// 创建 firekeeper 目录
// require('../lib/create-firekeeper')('simple')

// 创建 simple 目录
// require('../lib/create-simple')('simple')

// 创建 project 目录
// require('../lib/create-project')('./dist')

// 下载校验文件
// require('../lib/download-verify-file')('simple')

// 下载包
// require('../lib/download-package')('simple')

// 解压包
// require('../lib/unzip')('simple')